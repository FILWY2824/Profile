{
  "name": "alma-profile-portal",
  "version": "1.0.0",
  "private": true,
  "description": "A modular light glassmorphism portal with auth, RBAC, file-backed persistence, and env-driven configuration.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check:server": "node -e \"require('./server-app')\""
  }
}
